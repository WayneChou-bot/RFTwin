# Backend runtime deps for the Space. Looser than the repo's pins on purpose: the Space SDK also installs
# gradio[oauth,mcp] + spaces, whose mcp extra requires pydantic<=2.12.5 (the repo itself pins ~=2.13).
# Verified: pytest passes and exported schemas are byte-identical on pydantic 2.12.5.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
wsproto>=1.2,<2
pydantic>=2.11,<2.13
pyyaml>=6.0
httpx>=0.27
onnxruntime>=1.20
numpy>=2.0
pillow>=10
